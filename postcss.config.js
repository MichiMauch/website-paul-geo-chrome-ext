// This project uses plain CSS (design-system tokens), no PostCSS plugins.
// An explicit empty config stops PostCSS from walking up the directory tree
// and picking up a stray config in a parent folder.
export default {
  plugins: {},
};
